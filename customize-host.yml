---
- name: Customize a host
  hosts: localhost
  connection: local
  
  tasks:

    - name: set vi mode
      lineinfile:
        path: ~/.bashrc
        line: "set -o vi"

    - name: Start customizing vim
      lineinfile:
        path:     ~/.vimrc
        create:   yes
        line: |
          set tabstop=2
          set expandtab
